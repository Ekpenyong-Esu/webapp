/** A single navigation entry — an in-page anchor (single-page scroll) or a
 *  route path. Shared by the primary nav and the footer legal links. */
export type NavLink = {
  label: string;
  href: string;
};
